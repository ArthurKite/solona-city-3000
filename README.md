# Solona City 3000

Solona City 3000 is a single-page React web app exploring a fictional near-future city. It combines static city data with live API-driven details like weather and fun facts. The UI is centered around an interactive map and card layout, built with Vite and Tailwind CSS.

## Development

```bash
npm install
npm run dev
```
